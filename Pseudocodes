#IAs des chiens

# ========= BLUE =========
# Détermine l'index j du point d'intersection le plus proche dans le circuit c pour un chien de coordonnées (xc, yc)
Fonction inters_plus_proche(Circuit c, Entier xc, Entier yc) : Entier
    Entier d ← TAILLE_MAX
    Entiers i, x, y, d2, j

Début
    Pour i ← 0 à nombre_points(c) Faire :
        x ← coord_x(c, i)
        y ← coord_y(c, i)
        d2 ← distance(xc, yc, x, y)

        Si d2 < d Alors :
            d ← d2
            j ← i
        FinSi
    FinPour

Retourne j
Fin


# ========= YELLOW =========
#Variables externes :
# - circuit (liste de points)
# - target Node

Fonction Yellow(xc,yc,target):
# objectif = tuple (ou 2 vars) d'entiers qui est le point objectif que l'on enverra au seveur
# xc,yc entiers position du chien
# target Node de la cible (id,position), NULL si pas de cible
Debut
    Si target == NULL
        Si moutons_en_vue == NULL
            objectif = parcourir_circuit(xc,yc)
        Sinon
            target = moutons_en_vue[0]
            objectif = ramener_mouton(xc,yc,target)
    Sinon
        objectif = ramener_mouton(xc,yc,target)
Fin


# ========= PURPLE =========
